version: '3.8'

services:
  dingtalk-ad-sync:
    build: .
    container_name: dingtalk-ad-sync
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
